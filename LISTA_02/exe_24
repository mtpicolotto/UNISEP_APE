#include <stdio.h>

int main() {
    int n, i, primo = 1;
    scanf("%d", &n);

    for(i = 2; i < n; i++) {
        if(n % i == 0)
            primo = 0;
    }

    if(primo && n > 1)
        printf("Primo\n");
    else
        printf("Nao primo\n");

    return 0;
}