#include <stdio.h>

int main() {
    int n, maior = 0;

    do {
        scanf("%d", &n);
        if(n != 999 && n > maior)
            maior = n;
    } while(n != 999);

    printf("%d\n", maior);
    return 0;
}